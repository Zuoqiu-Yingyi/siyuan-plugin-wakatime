{
    "name": "wakatime",
    "type": "module",
    "version": "0.3.8",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "npm run build:plugin && npm run build:workers && npm run build:kernel",
        "build:dev": "vite build --mode plugin --sourcemap inline && vite build --mode kernel --sourcemap inline",
        "build:plugin": "vite build --mode plugin",
        "build:workers": "vite build --mode workers",
        "build:kernel": "vite build --mode kernel",
        "preview": "vite preview",
        "lint": "eslint --max-warnings 0 .",
        "format": "eslint --fix .",
        "check:svelte": "svelte-check --tsconfig ./tsconfig.json"
    },
    "dependencies": {
        "@workspace/components": "workspace:^",
        "@workspace/types": "workspace:^",
        "@workspace/utils": "workspace:^"
    }
}
